# Production Environment Variables for Vercel
# This file is used during production builds (npm run build)

# CRITICAL: Set VITE_BACKEND_URL to empty string so code uses fallback /api/chat
VITE_BACKEND_URL=

# OPENAI_API_KEY is set in Vercel dashboard → Settings → Environment Variables
# Do not put the actual key here
